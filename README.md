# dsh-council

A minimal Codex plugin scaffold for experimenting with council-style workflows in deepseek-harness.

## Status

The repository currently contains only the validated plugin manifest. Skills, MCP servers, apps, and runtime code will be added when their requirements are defined.

## Validate

```sh
python3 /Users/alex/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py .
```
